from django.db import models
# from account.models import Account
# from FCS import 
# # Create your models here.

# class Friends(models.Model):
# 	user_id = models.ManyToManyField(settings.AUTH_USER_MODEL,  related_name='user_id')
# 	friend_id = models.OneToOneField(settings.AUTH_USER_MODEL, )
# 	status = models.BooleanField(default=False)
# 	date_requested = models.DateTimeField(verbose_name="date requested", auto_now_add=True)
# 	date_confirmed = models.DateTimeField(verbose_name="date confirmed")

# 	def __str__(self):
# 		return self.user1_id
